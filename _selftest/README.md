# Selftest Project

A minimal project used to verify the unattended harness (auto-extend).

## Current version: v0.0.1

## Features

- Feature A: config parser (`config.py` / `load_config`) — reads sibling `config.json`, returns `{}` when missing
- Feature B: unit test coverage (`tests/test_config.py`) — run with `python -m unittest tests.test_config`
- Feature C: CLI entry point (`cli.py`) — `python cli.py --version` prints the README current version

## Roadmap (not implemented yet)

- (none)

## Notes

This README lists explicit unimplemented roadmap items so the agent can
plan follow-up tasks from the TODO queue.
