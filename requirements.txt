# curloop Python 依赖（npm postinstall 时 pip install 安装到嵌入式 Python）
# 核心：CDP WebSocket 通信 + 换号助手模板匹配点击 + REPL 补全
websockets>=12.0
pyautogui>=0.9.54
# Pillow 必须显式声明：pyscreeze（pyautogui 的截图依赖）的 Pillow 依赖在部分
# pip 解析下不会自动安装，缺失会导致运行时 "PyAutoGUI was unable to import pyscreeze"
Pillow>=10.0
# opencv-python 必需：pyscreeze 的 confidence 模糊匹配（换号模板定位）依赖 cv2，
# 缺失时 locateOnScreen(confidence=...) 直接抛 NotImplementedError
opencv-python>=4.8.0
prompt_toolkit>=3.0.36

# 可选（pywinauto UIA fallback；未安装时自动降级为仅 pyautogui 模板路径，可忽略）：
# pywinauto>=0.6.8
# comtypes>=1.4.0
# wmi>=1.5.1
